\section{Discussion}\label{sec:discussion}
This section reflects on the design choices made throughout the formalisation, discusses alternatives that were explored, and identifies 
limitations of the current mechanisation.

\subsection{Proof Architecture}
The direction of the typability argument determines the appropriate induction principle. To reconstruct a typed transition from a process step, the proof must follow the operational rule that was performed and then recover the compatible source typing rule. Accordingly, the current proof development for \texttt{typability\_subject\_reduction}$_\texttt{m}$ proceeds by induction on \texttt{ProcStep}$_\texttt{m}$ and applies typing inversion in each case. This choice is particularly important for parallel composition: the process transition identifies which component steps, whereas induction on the typing derivation initially exposes both typed components without determining which one must be transformed. The resulting architecture aligns the inductive cases with the observed operational behaviour and localises the remaining reconstruction work to the synchronisation and restriction cases.

The role of \texttt{EnvStep}$_\texttt{m}$ was also simplified during development. In an earlier formulation, the proof attempted to invert environment transitions in order to recover the hyperenvironment structure needed for typability. As a consequence, the environment relation had to carry additional freshness and structural premises so that inversion exposed the assumptions required by the corresponding hyperenvironment-extraction lemmas. These premises were useful for proof reconstruction, but were not intrinsic to the resource-level meaning of the environment transition itself. In the current formulation, \texttt{EnvStep}$_\texttt{m}$ is kept as a lightweight description of resource evolution, while the required freshness, linearity, and reconstruction information is supplied by \texttt{TypingStep}$_\texttt{m}$ and the typing invariants. The theorem \texttt{session\_fidelity\_env}$_\texttt{m}$ then projects typed transitions to their corresponding environment transitions.

\subsection{Proof-engineering trade-offs}
Several premises in the typing and transition relations are included for proof convenience rather than logical necessity. The most prominent example is the post-state disjointness proof \texttt{hD2} in the parallel constructors \texttt{par$_1$} and \texttt{par$_2$} of \texttt{TypingStep}$_\texttt{m}$. The former is shown in \cref{lst:typingstep-par1}. This premise is derivable from the pre-state disjointness proof \texttt{hD1}, the freshness condition requiring the names introduced by the label to be disjoint from the non-stepping component, and the structural property that transitions introduce only names recorded by their labels. Keeping \texttt{hD2} explicit avoids reconstructing this derived fact in every inductive case, at the cost of slightly cluttering the inductive definition. This is representative of a broader proof-engineering tradeoff: some redundancy in the definitions can make individual proofs shorter and more robust, even when it makes the relations less minimal.

A second source of proof overhead is the mismatch between concrete freshness witnesses in \texttt{ProcStep}$_\texttt{m}$ and co-finite premises in the typing rules. The binding constructors of \texttt{ProcStep}$_\texttt{m}$ record the particular names introduced by a transition, whereas the corresponding typing rules are stated co-finitely. The simulation proofs must therefore show that the co-finite typing premise can be instantiated with the concrete names chosen by the process transition. This is handled by auxiliary \emph{all fresh} lemmas, such as those for the \texttt{tensor} and \texttt{parr} prefix binders. Reformulating \texttt{ProcStep}$_\texttt{m}$ co-finitely would align the process and typing relations more directly and could reduce the need for these freshness-bridging lemmas, although its effect on the erasure simulation proofs remains to be checked.

\paragraph{Distribution of proof effort} The proof effort exposed by the mechanisation is concentrated less in transcribing the paper rules than in establishing the infrastructure required to use those rules inductively. One cluster concerns binding and local closure: the locally nameless representation requires opening and closing lemmas together with results such as \texttt{Typing\_preserves\_lc}, which recover well-scopedness properties from typing derivations. A second cluster concerns freshness transfer: co-finite typing premises must be instantiated with the concrete names exposed by process transitions, requiring the auxiliary \emph{all fresh} lemmas used in the typability argument. A third, and particularly substantial, cluster concerns resource permutation and reconstruction. Because environments and hyperenvironments are represented as lists while their logical composition is unordered, proofs must repeatedly extract and rearrange components using \texttt{List.Perm} and \texttt{HyperEnv.Perm}. This becomes especially demanding for transitions beneath restriction: after stepping the opened body, the proof must recover the endpoint-bearing components required to reconstruct the enclosing \texttt{cut} derivation. The incomplete \texttt{res} case of \texttt{typability\_subject\_reduction}$_\texttt{m}$ is the clearest remaining instance of this permutation-sensitive reconstruction problem.

\paragraph{Summary of shifted proof obligations} These concentrations of proof effort arise from deliberate choices in the representation and semantic architecture of the formalisation. The chosen representation avoids reasoning directly up to $\alpha$-equivalence and makes the derivation-level semantics structurally precise, but these benefits are accompanied by obligations concerning opening, freshness, permutation, and reconstruction. The principal instances of this shift are summarised in \cref{tab:proof-engineering-tradeoffs}.

\begin{table}[t]
\caption{Principal proof-engineering trade-offs in the mechanisation.}
\label{tab:proof-engineering-tradeoffs}
\centering
\begin{tabular}{@{}
  >{\raggedright\arraybackslash}p{0.43\linewidth}
  >{\raggedright\arraybackslash}p{0.49\linewidth}
@{}}
\toprule
\textbf{Mechanised design choice or simplification} & \textbf{Resulting proof obligation or trade-off} \\
\midrule
The locally nameless representation avoids explicit reasoning up to $\alpha$-equivalence for bound channel names. & Processes require opening and closing operations, local closure predicates, and lemmas connecting these operations with typing and transitions. \\
\addlinespace
Co-finite premises avoid fixing a particular fresh representative in rules that bind channel names. & Concrete fresh names selected by \texttt{ProcStep}$_{\texttt{m}}$ must be related back to the co-finite typing premises through all-fresh bridge lemmas. \\
\addlinespace
Environments and hyperenvironments are represented by lists while their logical composition is treated as unordered. & Exchange and component rearrangement require explicit \texttt{List.Perm} reasoning and the custom deep relation \texttt{HyperEnv.Perm}. In particular, the remaining \texttt{res} case requires reconstructing the two endpoint-bearing components needed to rebuild the enclosing \texttt{cut} derivation. \\
\addlinespace
Raw session-type expressions and process terms are represented independently of typing derivations, while \texttt{Typing} and \texttt{TypingStep}$_{\texttt{m}}$ represent valid typing derivations and their evolution. & Invariants such as local closure and linearity must be established from typing derivations, and the typability direction of session fidelity must reconstruct a derivation-level transition from a process-level step. \\
\addlinespace
A derivable post-state disjointness premise is retained explicitly in parallel transition constructors. & The transition relation is less minimal, but subsequent proofs can use post-transition linearity directly rather than reconstructing it repeatedly from auxiliary invariants. \\
\bottomrule
\end{tabular}
\end{table}

These trade-offs show that mechanisation does not merely replace informal arguments with automation. Avoiding explicit $\alpha$-equivalence simplifies the treatment of binders, while the representation of linear resources and derivation-level transitions makes permutation, freshness transfer, and structural reconstruction central proof obligations. A separate limitation, considered next, is that the formalisation does not currently include the process-level structural congruence used in the paper semantics.

\subsection{Structural congruence}
The formalisation does not include a structural congruence relation for processes. In the paper presentation of \pill{}, structural congruence identifies processes modulo the commutative, associative, and unit laws of parallel composition, together with scope manipulation for restriction. The mechanisation currently recovers the typing-level counterpart of these laws through the monoidal laws for hyperenvironments and the exchange rules of the typing relation. However, it does not prove that process syntax itself is closed under structural congruence. For example, processes such as $P \pp_p \nil$ and $P$ are not automatically interchangeable. The corresponding simplification must instead be reflected manually at the level of hyperenvironments using the monoidal identity lemmas.

This omission matters because the operational semantics is defined over the concrete syntax tree of processes. In particular, synchronisation rules apply to components exposed by the syntactic structure of parallel composition. Since synchronisation labels are binary, the two processes that synchronise must be visible as sibling components in the syntax tree. For example, in a process of the form $\phl{P \pp_p (Q \pp_p R)}$, a synchronisation between $\phl{P}$ and $\phl{Q}$ cannot be exposed by commutativity alone and would require associativity to rearrange the process into $\phl{(P \pp_p Q) \pp_p R}$. A full structural congruence development would therefore require proving that typing and transitions are preserved under an independent congruence relation. An alternative would be to build selected symmetry and reassociation principles directly into the transition relation, for example to account for the orientation of restricted endpoints or the nesting of parallel composition. Whether this would simplify the session-fidelity proofs and later behavioural theory, or merely shift the proof burden elsewhere, remains unexplored.

\subsection{Current limitations}
The static formalisation covers the full \pill{} calculus, while the operational semantics and metatheory currently target \pimll{}. Within this fragment, the only incomplete case of \path{typability_subject_reduction}$_\texttt{m}$ is \texttt{res}. As discussed in \cref{subsec:session-fidelity}, this case requires reconstructing the two post-transition hyperenvironment components carrying the restricted dual endpoints so that the enclosing \texttt{cut} derivation can be rebuilt. The proof attempt has isolated the missing infrastructure to single-block preservation and paired-endpoint reconstruction through typed transitions. The completed \texttt{tensor\_parr} extraction lemma in \cref{lst:app-extraction} illustrates the same form of permutation-sensitive component reconstruction for an internal communication case.

Several results from the paper metatheory also remain unmechanised, including the behavioural theory of bisimilarity and congruence, the diamond property, serialisation, non-interference, and readiness. These are discussed as directions for future work in the following section.
