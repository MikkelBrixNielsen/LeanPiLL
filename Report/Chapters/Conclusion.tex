% Paragraph 1: Motivation.
% This thesis set out to mechanise an existing \pill theory in Lean, validating a technically delicate paper presentation.

% Paragraph 2: Contributions.
% formalised syntax, locally nameless binding, session types, processes, environments, hyperenvironments, typing, operational semantics, and session-fidelity infrastructure.

% Paragraph 3: Metatheory status.
% Erasure projections and subject-reduction structure are established, typability is complete except \emph{res},  remaining gap is hyperenvironment reconstruction.

% Paragraph 4: Reflection.
% The mechanisation exposed proof-engineering tradeoffs: list-based environments, explicit disjointness, concrete vs co-finite freshness, structural congruence not mechanised.

% Paragraph 5: Closing.
% Even incomplete, the development validates large parts of the theory and identifies a clear path to completing \pimll and extending to full \piLL.

\section{Conclusion}\label{sec:conclusion}
This thesis set out to mechanise the syntax, semantics, and metatheory of \pill{} in Lean. The goal was not to extend the source calculus, but to validate and clarify an existing theoretical framework by making its binding structure, freshness assumptions, linear resource accounting, and operational metatheory explicit in a proof assistant.

The formalisation covers the static structure of the full calculus, including session types, processes, environments, hyperenvironments, and typing judgements. Processes are represented using a locally nameless encoding, while type variables are represented using De Bruijn indices. This avoids explicit reasoning about \(\alpha\)-equivalence while preserving readable free channel names in the mechanised syntax. The cost of this choice is the need for local-closure predicates, opening and closing operations, and co-finite reasoning principles.

The operational semantics is mechanised for the multiplicative fragment \pimll{} through three related transition systems: a typed transition system over typing derivations, a process transition system obtained by erasing typing resources, and an environment transition system describing the corresponding evolution of hyperenvironments. This mirrors the erasure structure of the source theory while adapting it to Lean's relational presentation of inductive transition systems.

The main metatheoretic development establishes the structure of the session-fidelity argument for the mechanised fragment. Typed derivation steps project to both process and environment transitions, and process transitions from well-typed sources can be lifted back to typed derivation steps in the completed multiplicative cases. Subject reduction is then obtained by composing this lifting result with the environment projection. The remaining proof obligation is the \texttt{res} case of \texttt{typability\_subject\_reduction}$_\texttt{m}$, where the proof must reconstruct the post-transition hyperenvironment after stepping under restriction.

The mechanisation also exposes several proof-engineering tradeoffs. Representing environments and hyperenvironments as lists makes structural induction and inversion feasible, but requires explicit permutation, disjointness, and freshness reasoning. Similarly, the mismatch between concrete freshness witnesses in \texttt{ProcStep}$_\texttt{m}$ and co-finite premises in the typing rules introduces auxiliary proof obligations. Finally, the absence of process-level structural congruence keeps the transition systems syntax-directed, but requires some rearrangements to be handled manually through exchange rules and hyperenvironment permutation lemmas.

Overall, the development provides a substantial mechanised account of the static structure of full \pill{} and the dynamic metatheory of its multiplicative fragment. It confirms that the central erasure and session-fidelity architecture of the source theory can be represented in Lean, while isolating the remaining proof infrastructure needed to complete the \pimll{} development and extend the mechanisation to the full calculus.













% NOTE: Since I am also supposed to hand in the entire codebase I think it would makes sense to include inductive definitions in full (more or less) and maybe include the larger theorems like session fidelity and stuff in an abbreviated form or something and then just keep these still relevant metatheoretic results to just being inline signatures and some prose.






% TODO: Look at typability section again in metatheory section.

% TODO: Rephrase the first paragraph in Evolution of the session fidelity proof



% TODO: Mention that one could also align the formulation of the Typing relation having co-finite quantification as "\forall x \notin L" and the TypingStep relation having "\forall x, x \notin L"









% TODO: Conclusion
% TODO: Abstract(s)


% TODO: Should there be a specific related work section it seems covred by the background, binding theory and architecture?



% TODO: De duplicate the discussion and future work sections
% TODO: Should the future work paragraphs be subsections instead, same with discussion.


% TODO: Check bindingrepresentation includes reasoning for choice, benefits, drawbacks and alternatives:
  % De Bruijn for types
  % Locally Nameless for processes
  % Co-finite quantification (strong induction principle, simplification in typing and reduction proofs)

% TODO: Generally check if disposiiton matches

% TODO: Check if proof strategy description and comparison seems deep enough.

% TODO: Show more synax in the process section?
% TODO: Keep or delete comment on notation for Env.merge / say something in prose about it actually being \glq to avoid colliding with how lean interprets commas normally

% TODO: Include githash for final commit before report submission
% TODO: Update the README on github


% TODO: Add notation paragraph to processes along the lines of this:
	% \paragraph{Notation} \path{STypes/Notation.lean} introduces notation mirroring the mathematical presentation from \cref{subsec:types}. The 		only divergence is that exponentials are written {$\thl{\bang\bang A}$} and {$\thl{\query\query A}$} to avoid clashes with 		Lean syntax.
	% Also add notation explanation for \_h or replace with just |
	% The main notation clarification would probably be the use of double brackets and parentheses.

% TODO: Replace commas with /-‚-/ in listings and ‚ in prose regarding Env.merge

% TODO: Do a sanity check on this paragraph:
% - Of particular importance are the commutation lemmas between opening and substitution:
% 	open_substNames_comm establishes that opening and name substitution commute, and the corres-
% 	ponding lemma for type substitution ensures that substituting under a binder produces the same
% 	result regardless of order. These properties are essential in the typability proof, where the co-finite
% 	witnesses produced by the typing rules must be reconciled with the concrete names introduced by
% 	the process transitions. lines 1186-1191 just before subsection 4.1.3
% - This formulation differs from the paper presentation, which formulates erasure extensionally,
% 	using set-valued transition functions and equality of successor sets.

% TODO: Change typing context -> typing environment

% TODO: Mention somewhere that the formalised typing rules have inverted ordering so instead of taking elements from the back they take them from the front / head of the list (with some exceptions)

% TODO: Replace bold nu instance with $\mathit{v}$

% TODO: Check if Types.subst notation is mentioned anywhere 

% TODO: Flesh out the proof strategies used when mentioning key lemmas.

% TODO:
% - Redo the structural invariants section for processes in the formalisation section to talk about something like this instead of what is currently there.
% 	Proc.not_mem_f_open - Typability
% 	Proc.f_open_erase, Proc.f_open_two_erase - Typing_f_eq_names
% 	Proc.lc_of_open_two, Proc.lc_of_open_gen - Typing_preserves_lc_proc to get Typing_preserves_lc
% 	Proc.open_substNames_comm, Proc.openCut_substNames_comm - Typing_substNames
% 	Proc.openCut_substTypes_comm, Proc.open_substTypes_comm_notation- Typing_substTypes
% 	Proc.shiftTypes_openCut_comm, Proc.shiftTypes_open0_comm - Typing_weakening (Decide if talking about this)

% - Look at typability section again in metatheory section.
% - Rephrase the first paragraph in Evolution of the session fidelity proof

% Since I am also supposed to hand in the entire codebase I think it would makes sense to include inductive  definitions in full (more or less) and maybe include the larger theorems like session fidelity and stuff in an abbreviated form or something and then just keep these still relevant metatheoretic results to just being inline signatures and some prose?  









% FIXME: Something funky is going on with the outlin for the Types.dual definitions and the table showing the correspondence between paper, LN and Lean types. Generally check all the listings to see if the outline does anything weird. If this is hard to fix maybe just remove them?

% Check that citations use the correcet style depending on sentence structure
% Fix all remaining FIXMEs and TODOs
% Resolve broken cross-references
% British spelling pass
% Consistency in calling type out put a payload type or just type. Do the same for paylod name / name
% Remove highlighting from prose
% Check all \thl and \phl are inside $...$
% AI declaration
% Turn remaining Note paragraphs into remark environments
% GitHub source code link
% Disable line numbers