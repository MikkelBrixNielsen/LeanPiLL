% Refer back to paper syntax and definitons - Don't repeat what has already been written instead use what has been defined as basis and show important differences in how the formalization differes from the paper syntax.

% This chapter is supposed to be where background binding representation and the $\pi$LL syntax comes together to creates a mechanization of the pre established theory. Should probably include:
  % How Processes are defined in Lean and how LN has influences the constructors
  % How Types are defined in Lean and how De Bruijn influced the constructors
  % How Processes and Types are used together to define the Typing system, as well as what influence LN and De Bruijn had on the constructors in the form of including co-finite quantification.
  % Define the operational semantics for the Typing sytem and how it can be mapped to an operational semantic for Processes and Types, resepctively.
  % Mention the elimination of Alpha equivalence, (Would be good to have a proof that it has been reduced to structural equivalence)

% When talking about the formlization, maybe show a Lean version of a mapping to the reports running example, and comment on how, due to it being a strict application, there are sometimes empty hyperenv or env artifacts which roam around the derivation. Contextualize this in process congruence and the monoidal structure of typing environments, and mention that this property has yet to be added to the formalization.
  % Maybe something along the lines of:
  % Notice that in the intermediate transition, the strict application of the \textsc{Mix} rule yields the typing context $\emptyset \mid y:1$. By maintaining the structural footprint of the terminated left-hand process ($\vdash \textbf{0} :: \emptyset$), the exact correspondence with the parallel transition rule remains visually explicit. Following the final $y[]$ transition, the process fully terminates as $\textbf{0} \mid \textbf{0}$. At this stage, we can apply process congruence ($P \mid \textbf{0} \equiv P$) alongside the monoidal identity laws of the hyperenvironment ($\emptyset \mid \emptyset = \emptyset$) to cleanly resolve the derivation to its final state: $\vdash \textbf{0} :: \emptyset$


% ---------- SPLIT ---------- %

% Write about Core system, syntax overview, process structure, judgements, operational semantics etc. with focus on the multiplicative component.

% 3. πLL System
    % 3.1 Syntax (full system)
    % 3.2 Types (full system)
    % 3.3 Typing (full system)
    % 3.4 Operational Semantics (multiplicative fragment)

% Generally
    % Present syntax for processes and types
    % Show some Typing rules
    % For operational semantics, show TypingStep 
        % Define process and types semantics 
        % Make this very breif


% \section{$\pi$MLL: Paper Level Definitions and Syntax}
% ------- WIP
% \section{Operational Semantics}

% \paragraph{Fragment decomposition.}
% The system \pill can be naturally decomposed into two complementary fragments. The first is the multiplicative fragment, \pimll, which includes constructs such as tensor, par, and their associated units. This fragment captures the core structure of interaction, describing how processes communicate and synchronize through linear channels. The second is the non-multiplicative fragment, comprising additives, exponentials, and quantifiers. These constructs enrich the language with additional expressive power, enabling features such as internal and external choice, controlled duplication and disposal of resources, and polymorphic session behavior.

% In this development, the syntactic and typing components are defined for the full \pill system, encompassing both fragments. However, the operational semantics and associated metatheory are established for the multiplicative fragment, which suffices to capture the essential communication behavior underlying the system. Extending the operational treatment to the non-multiplicative fragment introduces additional considerations, such as choice resolution and resource management, and is left for future work.

% --- MERGE THE TWO THINGS ABOVE --- %

% This was done in section 3, so whenever needed reference that section.
% Aim to bridge theory-code gap and explain design choices
  % 1. Design goals
  % 2. LN for processes (how does this eliminates AlphaEq)
  % 3. De Bruijn indices for types (needed type variables in first iteration to be locally closed, so instead of assuming this as hypothesis, implemented De Bruijn indices and the notion of a type being locally closed based on the current binder level for a typing derivation then 'n' in front of judgements)
  % 4, Co-finite quantification 

% Commands for double brackets and syntax prefixes
\newcommand{\dbrack}[1]{[\![#1]\!]}
\newcommand{\dparen}[1]{(\!(#1)\!)}
\newcommand{\dangle}[1]{\langle\!\langle #1 \rangle\!\rangle}
\newcommand{\dsend}[3]{#1\dbrack{#2}.#3}
\newcommand{\drecv}[3]{#1\dparen{#2}.#3}
\newcommand{\dserver}[3]{#1\dangle{#2}.#3}
\newcommand{\dlsend}[2]{#1\dbrack{#2}}
\newcommand{\dlrecv}[2]{#1\dparen{#2}}
\newcommand{\dlserver}[2]{#1\dangle{#2}}

\section{Formalisation}\label{sec:formalisation}
This section describes the mechanisation of \pill{} in Lean 4, bridging the theoretical foundations developed in~\cref{sec:background,sec:binding-architecture} and their concrete implementation. The formalisation is organised into three top-level namespaces: \texttt{PiLL.Model}, which defines the static structure of the calculus, \texttt{PiLL.Semantics}, which defines its operational behaviour, and \texttt{PiLL.SessionFidelity}, which establishes the metatheoretic results connecting the two. Each component is presented in dependency order, highlighting design decisions and divergences from~\citet{abs-2106-11818}.

\subsection{Model}\label{subsec:model}
The \texttt{Model} namespace covers the full \pill{} calculus and is structured around the layers introduced in~\cref{sec:background}. \texttt{Base.lean} provides shared syntactic infrastructure used across the model. Session types are defined in \texttt{STypes/}, processes in \texttt{Processes/}, and typing contexts in \texttt{Environments/} and \texttt{HyperEnvironment/}. Typing judgements and their properties are collected in \texttt{Judgement/}, which includes inversion lemmas, linearity properties, substitution, and the co-finite freshness infrastructure described in~\cref{subsec:co-finite-quantification}.

% FIXME: How much of this is relevant, and maybe actually just mention the specifics when they become relevant throughout the other sections?
% \paragraph{Base}\label{paragraph:base}
% This file \texttt{Base.lean} contains the definitions for the shared operational interfaces used throughout the model as Lean typeclasses. The typeclasses include instances for shifting, substituting, opening and closing of names and types, as well as a few typeclases for having shared syntax between process and labels. For example, depending on context the notation $\dlsend{x}{y}$ can be use to prefix a process, in which case it evaluates to the send constructor, or it can be used as a label on a transition, in which case it would evaluate as a label instead. The same syntax is used leaving the the payload empty for refering to the empty send and its respective label. This pattern is repeated using $\dlrecv{x}{y}$ for both recieve variants as well. Notably, two classes are needed to define shifting for types and names, respectively, as the signature takes the subject, cut-off depth, and by how much to increment, which isn't enough to determine whether it should be names or types which is shifted.
% \begin{verbatim}
% class HasShiftNames (Subject : Type) where
%   shift : Subject → Nat → Nat → Subject
% class HasShiftTypes (Subject : Type) where
%   shift : Subject → Nat → Nat → Subject
% class HasOpenTwo (Subject R1 R2 Target : Type) where 
%   open_ : Subject → R1 → R2 → Target → Subject
% \end{verbatim}

% FIXME: never mentioned the atom and atomDual constructors, also describe the reason that they exist.
\subsubsection{Session Types}\label{subsubsec:stypes}
Session types are defined in \texttt{STypes/Basic.lean} as the inductive datatype \texttt{Types}. Following the locally nameless syntax established in \cref{subsec:locally-nameless}, type variables are isolated into a separate inductive type, \texttt{TVar}, which explicitly distinguishes between free and bound occurrences:
%
\begin{lstlisting}[language=Lean4]
inductive TVar : Type
  | free  (x : FTVar)   
  | bound (x : BTVar)   
\end{lstlisting}
%
Here, \texttt{FTVar} represents a free type variable identified by a natural number, while \texttt{BTVar} represents a bound type variable encoded as a De Bruijn index.

The remaining constructors directly mirror the theoretical grammar of the calculus. In addition, the formalisation introduces constructors for uninterpreted atomic base types (\texttt{atom} and \texttt{atomDual}), which provide the base cases for the inductive structure of the syntax. \Cref{tab:types-comparison} illustrates the correspondence between the paper syntax, the locally nameless representation, and the Lean implementation.

\begin{highlight}
\begin{table}[htbp]  
    \centering
    \caption{Comparison of standard, locally nameless, and Lean representations for \pill{} types.}
    \label{tab:types-comparison}
    \begin{tabular}{llll}
        \toprule
        \textbf{Standard} & \textbf{Locally Nameless} & \textbf{Lean Constructor} & \textbf{Binds} \\
        \midrule
        $\thl{A \tensor B}$ & $\thl{A \tensor B}$         & \texttt{| tensor (A B~: Types)} & None \\
        $\thl{1}$           & $\thl{1}$                   & \texttt{| one}                  & None \\
        $\thl{\forall X.A}$ & $\thl{\forall. A}$ & \texttt{| forall\_ (A~: Types)} & 1 type \\
        $\thl{\exists X.A}$ & $\thl{\exists. A}$ & \texttt{| exists\_ (A~: Types)} & 1 type \\
        $\thl{X}$           & $\thl{X}$                   & \texttt{| var (v~: TVar)}       & None \\
        $\thl{X^\bot}$      & $\thl{X^\bot}$              & \texttt{| varDual (v~: TVar)}   & None \\
        \bottomrule
    \end{tabular}
\end{table}
\end{highlight}

Notably, the polymorphic binders \hl{$\thl{\forall.A}$} and \hl{$\thl{\exists.A}$} reflect the depth-indexed approach detailed in \cref{subsec:co-finite-quantification}. The constructors keep the bound De Bruijn index implicit, taking only the continuation body \texttt{A} as an argument (an underscore ``\texttt{\_}'' has been appended to the constructor names to prevent namespace collisions with Lean's built-in \texttt{forall} and \texttt{exists} keywords).

\paragraph{Duality} \texttt{Types.dual} implements duality as a structurally recursive function, mapping each constructor to its dual. For example:

\begin{lstlisting}[language=Lean4]
def Types.dual : Types -> Types
  | .tensor A B  => .parr   (dual A) (dual B)
  | .forall_ A   => .exists_ (dual A)
  | .one         => .bot
  | ...
\end{lstlisting}

% FIXME: If anything in this paragraph isn't mentioned later remove it. (needs phrasing corrections)
% FIXME: should this mirror the exact syntax instead of the placeholder syntax.
% The fundamental property that duality is an involution, \hl{$\thl{{(A^\bot)}^\bot = A}$}, is proved in \path{STypes/Lemmas.lean} by structural induction. Furthermore, because duality interacts deeply with the depth-indexed binding architecture, the formalisation establishes a suite of essential commutation lemmas. Crucially, we prove that duality preserves local closure \hl{($\thl{A}.\texttt{lc} \iff \thl{A^\bot}.\texttt{lc}$)}, and that it commutes with both De Bruijn shifting \hl{($\thl{\shiftdc{d}{c}(A^\bot) = (\shiftdc{d}{c}A)^\bot}$)} and substitution \hl{($\thl{B^\bot\{A/k\}} = \thl{(B\{A/k\})^\bot}$)}. It has also been established that duality is injective \hl{($\thl{A^\bot} = \thl{B^\bot} \iff \thl{A} = \thl{B}$)}. %FIXME: Specifically this last part about injectivity is probably not used.

The fundamental property that duality is an involution,
\hl{$\thl{{(A^\bot)}^\bot = A}$},
is proved in \path{STypes/Lemmas.lean} by structural induction.

The formalisation additionally establishes that duality preserves local closure and commutes with both shifting and substitution:
\begin{highlight}  
\begin{gather*}
\thl{A}.\texttt{lc} \iff \thl{A^\bot}.\texttt{lc} \qquad
\thl{\shiftdc{d}{c}(A^\bot) = {(\shiftdc{d}{c}A)}^\bot} \qquad
\thl{B^\bot\{A/k\}} = \thl{{(B\{A/k\})}^\bot}
\end{gather*}
\end{highlight}

\paragraph{Local closure} \texttt{STypes\slash{}Properties.lean} defines local closure for types as a depth-indexed predicate \texttt{Types.lc (k~: Nat)~: Types $\to$ Prop}, where the depth parameter $k$ tracks how many binders the recursion has traversed. 

At the leaves of the syntax tree, local closure is evaluated by the helper predicate \texttt{TVar.lc}. Because free variables are explicit they do not contain any indices, as such they are inherently locally closed at any depth. Conversely, a bound variable is only locally closed if its De Bruijn index $i$ is strictly less than the current depth $k$, ensuring it points to an enclosing scope:

\begin{lstlisting}[language=Lean4]
def TVar.lc : Nat -> TVar -> Prop
  | _, .free _   => True
  | k, .bound i  => i < k
\end{lstlisting}

The primary \texttt{Types.lc} predicate is then defined by structural recursion. For non-binding constructors, the depth parameter distributes across the sub-terms. When a variable is encountered, the predicate delegates the check to \texttt{TVar.lc}. When descending into the body of a polymorphic binder, the depth counter increments by one, bringing the newly introduced index safely into scope:

\begin{lstlisting}[language=Lean4]
def Types.lc : Nat -> Types -> Prop
  | _, .one         => True
  | k, .var v       => TVar.lc k v
  | k, .tensor A B  => A.lc k ∧ B.lc k
  | k, .forall_ A   => A.lc (k + 1)
  | k, .exists_ A   => A.lc (k + 1)
  | ...
\end{lstlisting}

Finally, a type is defined as \emph{closed} if it contains no dangling indices at the top level, meaning it is locally closed at depth $0$. This is registered as the abbreviation \texttt{Types.lc\_0}.

\paragraph{Shifting} \path{STypes/Shift.lean} defines shifting following the standard De Bruijn shifting operation introduced in \cref{subsec:co-finite-quantification}. The operation traverses the syntax tree structurally using a cutoff depth $d$ and a shift amount $c$.

For standard connectives, shifting distributes recursively across subterms, while base constructors such as \texttt{one} and \texttt{bot} remain unchanged. When traversing a polymorphic binder like \texttt{forall\_}, the cutoff depth increments by one to account for the newly entered scope:

\begin{lstlisting}[language=Lean4]
def Types.shift (d c : Nat) : Types -> Types
  | .var v       => .var (v.shift d c)
  | .tensor A B  => .tensor (A.shift d c) (B.shift d c)
  | .forall_ A   => .forall_ (A.shift (d + 1) c)
  | t            => t
\end{lstlisting}

At the variable level, free variables remain unchanged. For bound indices, the function compares the De Bruijn index $i$ against the cutoff depth $d$. Indices satisfying $i < d$ are locally bound within the traversed scope and therefore remain unchanged. Otherwise, the index is incremented by $c$ to preserve its reference to the surrounding environment:

\begin{lstlisting}[language=Lean4]
def TVar.shift (d c : Nat) : TVar -> TVar
  | .bound i => if i < d then .bound i else .bound (i + c)
  | .free n  => .free n
\end{lstlisting}

\paragraph{Substitution} \path{STypes/Subst.lean} defines substitution as the eager substitution of $A$ for a target De Bruijn index $k$ within a host type $B$, denoted as $B\{A // k\}$. 

At the variable level, the function evaluates the bound De Bruijn index $i$ against the target depth $k$. If $i = k$, the target has been reached, and the variable is replaced by the instantiated type $A$. If $i < k$, the variable is bound by an inner scope and is unaffected. Finally, if $i > k$, the index refers to an outer binder beyond the substituted variable. Since substitution removes the binder corresponding to $k$, these indices are decremented by one to preserve their relative distance to the surrounding environment.

\begin{lstlisting}[language=Lean4]
| .var (.bound i) =>
    if i == k then A
    else if i > k then .var (.bound (i - 1))
    else .var (.bound i)
\end{lstlisting}

This operation is lifted to the \texttt{Types} level via structural recursion. For standard connectives, the substitution simply distributes across the sub-terms. However, when descending into a polymorphic binder such as \texttt{forall\_}, two critical protective adjustments must occur. First, the target depth $k$ increments by one ($k + 1$) to account for crossing the new binder. Second, the payload type $A$ is explicitly shifted (\texttt{shift 0 1 A}) to ensure that any free variables residing inside $A$ are not accidentally captured by this newly entered scope:

\begin{lstlisting}[language=Lean4]
| .forall_ B => .forall_ (B.subst (shift 0 1 A) (k + 1))
\end{lstlisting}

\paragraph{Notation} \path{STypes/Notation.lean} defines notation following the mathematical conventions established in \cref{subsec:types} and extended in \nameref{par:ln-syntax-types}. The only noteworthy divergence is that the exponentials \hl{$\thl{\bang A}$} and \hl{$\thl{\query A}$} are written \hl{$\thl{\bang\bang A}$} and \hl{$\thl{\query\query A}$} respectively, to avoid clashing with Lean's built-in $\bang$ (boolean negation) and $\query$ (syntactic hole) operators.

The full inductive definition of \texttt{Types} and all lemmas from \path{STypes/Lemmas.lean} are listed in \cref{app:stypes}. % FIXME: Reference to appendix create appendix

% FIXME: Make reference back to the section defining locally nameless for processes.
% FIXME: Reference to section talking about how the binders work
% FIXME: Last sentece could be phrased better.
% FIXME: add references to section describing the contents of Model/Processes/Fresh.lean (replace the inline path? or maybe have together with the inline path?)
\subsubsection{Processes}\label{subsubsec:processes}
Processes are defined in \path{Processes/Basic.lean} as the inductive type \texttt{Proc}. Following the established locally nameless architecture, channel references are partitioned into a dedicated \texttt{Channel} inductive. Explicit free channels are instantiated as concrete natural numbers (\texttt{FPName}). While the raw process syntax permits arbitrary reuse of these numbers, using \texttt{Nat} allows the formalisation to systematically compute strictly fresh names by incrementing the supremum of any finite set of existing names (mechanised in \path{Processes/Fresh.lean}). Conversely, bound channels are represented using De Bruijn indices, encoding their relative distance to the corresponding binder.

\begin{lstlisting}[language=Lean4]
inductive Channel : Type where
  | free  (x : FPName)
  | bound (x : BPName)
\end{lstlisting}

% FIXME: Phrasing of the last sentence
As discussed in \cref{subsec:locally-nameless}, binding constructs in the process syntax no longer carry explicit payload variables. Instead, bound occurrences are captured purely by their relative position. Comparing the standard paper grammar with the intermediate locally nameless notation and the final constructors of the \texttt{inductive Proc} type illustrates this abstraction directly:

\begin{highlight}  
\begin{center}
\begin{tabular}{llll}
    \toprule
    \textbf{Standard} & \textbf{Locally Nameless} & \textbf{Lean Constructor} & \textbf{Binds} \\
    \midrule
    $\phl{\send{x}{y}{P}}$ & $\phl{\send{x}{\bullet}{P}}$ & \texttt{| tensor (x~: Channel) (P~: Proc)} & 1 channel \\
    $\phl{\recv{x}{y}{P}}$ & $\phl{\recv{x}{\bullet}{P}}$ & \texttt{| parr (x~: Channel) (P~: Proc)} & 1 channel \\
    $\phl{\res{xy}{P}}$ & $\phl{\res{(\bullet, \bullet)}{P}}$ & \texttt{| cut (P~: Proc)} & 2 channels \\
    $\phl{\recvtype{x}{X}{P}}$& $\phl{x(\diamond).P}$ & \texttt{| input (x~: Channel) (P~: Proc)} & 1 type \\
    $\phl{\clientdup{x}{x'}{P}}$ & $\phl{\clientdup{x}{\bullet}{P}}$ & \texttt{| duplicate (x~: Channel) (P~: Proc)} & 1 channel \\
    \bottomrule
\end{tabular}
\end{center}
\end{highlight} 

Notice that \texttt{cut} takes only a continuation \hl{$\phl{P}$} with no explicit channel arguments, as both dual endpoints are simultaneously bound as indices $0$ and $1$ within the scope of \hl{$\phl{P}$}. Similarly, for constructs like \texttt{tensor}, \texttt{parr}, and \texttt{input}, the subject channel $x$ over which the interaction occurs is kept explicit, while the transmitted payload is anonymized into the continuation.

\paragraph{Opening and closing} \path{Processes/OpenClose.lean} defines opening and closing operations following the Locally Nameless principles established in \cref{subsec:opening-and-closing}. 

Because binders are represented positionally, the depth parameter $k$ increments according to the number of channels bound by each constructor. For standard communication prefixes like \texttt{tensor}, \texttt{parr}, and the server \texttt{duplicate} operation, exactly one channel is bound, incrementing $k$ by one. The restriction constructor (\texttt{cut}) simultaneously binds two dual endpoints, requiring $k$ to increment by two. Notably, the \texttt{input} constructor binds a type variable rather than a channel. Consequently, it does not affect the channel depth parameter $k$.

\begin{lstlisting}[language=Lean4]
def Proc.open (P : Proc) (u : Channel) (k : Nat) : Proc :=
  match P with
  | .tensor x P     => .tensor (x.open u k) (P.open u (k + 1))  
  | .parr   x P     => .parr   (x.open u k) (P.open u (k + 1))  
  | .cut      P     => .cut                 (P.open u (k + 2))
  | .input  x P     => .input  (x.open u k) (P.open u k)
  | ...
\end{lstlisting}

At the leaves of the syntax tree, this recursive traversal delegates to the base case defined by \texttt{Channel.open}. For bound channels, the function evaluates the De Bruijn index $i$ against the target depth $k$, replacing it with the instantiated channel $v$ upon an exact match. Explicit free channels remain entirely unaffected:

\begin{lstlisting}[language=Lean4]
def Channel.open (u v : Channel) (k : Nat) : Channel :=
  match u with
  | bound i => if i == k then v else bound i
  | c => c
\end{lstlisting}

For restriction, which requires instantiating two endpoints simultaneously, the \texttt{HasOpenTwo} typeclass from \path{Base.lean} is instantiated by opening sequentially: first replacing index $k+1$ with the second endpoint $v$, then replacing index $k$ with the first endpoint $u$:

\begin{lstlisting}[language=Lean4]
instance : HasOpenTwo Proc Channel Channel Nat where
  open_ P u v k := (Proc.open (Proc.open P v (k + 1)) u k)
\end{lstlisting}

To complete the bidirectional infrastructure, \texttt{Proc.close} and \texttt{Channel.close} define the exact inverse operation. Used primarily when abstracting explicit free names into new scopes, \texttt{Proc.close} traverses the process tree using a structural recursion identical to \texttt{Proc.open}, applying the exact same depth-incrementing rules for single and double binders. When the recursion reaches a leaf node, it delegates to \texttt{Channel.close}. If the leaf is a free channel (\texttt{FPName}), the function compares it against the target name $x$, upon a match, the explicit name is \emph{abstracted} into a bound De Bruijn index at the accumulated depth $k$:

\begin{lstlisting}[language=Lean4]
def Channel.close (u : Channel) (x : FPName) (k : Nat) : Channel :=
  match u with
  | .free name => if x == name then .bound k else .free name
  | .bound i   => .bound i
\end{lstlisting}

\paragraph{Local closure} \path{Processes/LocalClosure.lean}
defines local closure for processes as a two-parameter predicate \texttt{Proc.lc (k n~: Nat)~: Proc $\to$ Prop}. Because the calculus features a two-sorted syntax, the predicate must maintain independent depth counters: $k$ tracks the depth of channel binders, while $n$ tracks the depth of type binders. This ensures that when a process embeds a type, such as the payload $A$ in an output operation, the embedded type can be correctly evaluated against the current type depth $n$ during its own local closure check.

The structural recursion evaluates each constructor by incrementing the appropriate counter based on the number and sort of binders introduced. For instance, a \texttt{tensor} prefix binds a single continuation channel, incrementing the channel depth to $k+1$. The restriction operation (\texttt{cut}) binds two dual endpoints simultaneously, incrementing the depth to $k+2$. Conversely, an \texttt{input} prefix binds a polymorphic type variable rather than a channel. This leaves the channel depth $k$ unchanged while incrementing the type depth to $n+1$:

\begin{lstlisting}[language=Lean4]
def Proc.lc : Nat -> Nat -> Proc -> Prop
  | k, n, .tensor x P  => x.lc k ∧ P.lc (k + 1) n
  | k, n, .cut P       => P.lc (k + 2) n
  | k, n, .input x P   => x.lc k ∧ P.lc k (n + 1)
  | ...
\end{lstlisting}

A process is considered globally \emph{closed} when it contains no dangling indices of either sort, meaning \texttt{Proc.lc 0 0} holds. This is registered as the abbreviation \texttt{Proc.lc\_0}.

\paragraph{Free names} \path{Processes/Names.lean} defines \texttt{Proc.f~: Proc $\to$ Finset FPName} to compute the free names of a process. It does this by traversing the process, collecting all explicit channel identifiers into a finite set. Because locally nameless binders are represented by anonymous indices, they contribute nothing to this set. Consequently, constructors like \texttt{cut}, which operate entirely on bound endpoints and take no explicit subject channels, simply pass through the free names of their continuation:

\begin{lstlisting}[language=Lean4]
def Proc.f : Proc -> Finset FPName
  | .cut P    => P.f
  | .par P Q  => P.f ∪ Q.f
  | .link x y => x.f ∪ y.f
  | ...
\end{lstlisting}

\paragraph{Freshness} \path{Processes/Fresh.lean} establishes freshness to support the generation of globally unique names. Given a finite set $L$ of forbidden names, a strictly fresh name is deterministically computed as $\max(L) + 1$. The formalisation provides two critical lemmas, \texttt{exists\_one\_fresh} and \texttt{exists\_two\_fresh}, which guarantee the generation of one or two distinct names residing entirely outside of $L$. These lemmas are essential for providing witnesses to satisfy the co-finite quantification premises used throughout the typing and transition rules, particularly for restriction, which requires introducing two fresh, non-colliding endpoints simultaneously.

\paragraph{Name substitution} \path{Processes/SubstNames.lean} establishes the machinery for name substitution as \texttt{Proc.substNames (R T~: FPName)~: Proc $\to$ Proc}, replacing all explicit free occurrences of the target name $T$ with the replacement name $R$. Because the locally nameless architecture strictly partitions free names and bound indices into disjoint syntactic categories, substituting a free name $R$ only interacts with other explicit free names at the leaves of the syntax tree. Binders, which operate exclusively via relative De Bruijn indices, are fundamentally blind to the concrete name $R$. Therefore, this operation is intrinsically capture-avoiding by construction, completely eliminating the need for complex freshness constraints or $\alpha$-renaming during substitution.

\paragraph{Type substitution} \path{Processes/SubstTypes.lean} establishes the machinery needed for type substitution as \texttt{Proc.substTypes (A~: Types) (k~: Nat)~: Proc $\to$ Proc}, eagerly substituting $A$ for the De Bruijn index $k$ throughout a process. 

Because the syntax is two-sorted, the operation delegates to the type-level substitution machinery whenever it encounters an embedded type. For example, within an \texttt{output} prefix, \texttt{Types.subst} is applied directly to the transmitted type payload.

Because the syntax is two-sorted, the operation recursively lifts to the type-level substitution \texttt{Types.subst} whenever an embedded type is encountered. For example, within an \texttt{output} prefix, \texttt{Types.subst} is applied directly to its payload.

When traversing an \texttt{input} prefix, which binds a type variable, the substitution descends with an incremented depth $k + 1$. Simultaneously, $A$ is explicitly shifted (\texttt{A.shift 0 1}) to prevent its free variables from being captured by the newly entered binder, mirroring the pattern used in \texttt{Types.subst} from \cref{subsubsec:stypes}:

\begin{lstlisting}[language=Lean4]
| .input x P => .input x (P.substTypes (A.shift 0 1) (k + 1))
\end{lstlisting}

A consequence of the locally nameless representation is that no channel shifting operation (e.g. \texttt{shiftNames}) is required. In a pure De Bruijn encoding such as that used for \texttt{Types} in \cref{subsubsec:stypes}, variables are represented as relative indices and must be shifted when traversing binders to preserve correct binding structure. By contrast, free channels (\texttt{FPName}) are represented as global identifiers rather than relative positions, and are therefore unaffected by binder depth. This avoids the need for structural adjustment of channel names during substitution, simplifying the process-level infrastructure.

\paragraph{Structural Properties} \path{Processes/Lemmas.lean} establishes the fundamental structural properties required by the later typing and operational metatheory. This includes commutation lemmas for interacting operations (e.g., \texttt{open\_substNames\_comm}, showing that opening commutes with name substitution), preservation properties for local closure (e.g., \texttt{lc\_of\_open}), and lemmas relating binders to free-name analysis (e.g., \texttt{f\_close}, proving that closing removes a free name from the resulting term).

These results provide the core infrastructure required for the typing and transition semantics developed in subsequent chapters.

For completeness, the full inductive definition of \texttt{Proc} and all lemmas from \path{Processes/Lemmas.lean} are listed in \cref{app:processes}. % FIXME: Make an appendix for this.




% \subsubsection{Environments and Hyperenvironments}\label{subsubsec:envs}
% The inductives, merging, disjointness condition
% How has the partial commutative monoid structure been achieved in Lean
% Any interesting divergence from the paper

\subsubsection{Environments}\label{subsec:environments}
The environment, tracks the free channels available to a process and their assigned session types in an unordered set. However, formalising this directly in Lean requires a more concrete data structure.
% FIXME: Maybe this is a relevant spot to mention that quotients are a paint to deal with and there have been a few other iterations, one using finsets leading to quotient hell, then one trying to use alists resulting in the same fate, before finally getting to lists, using permutation as an invariance to recover the PCM structure, then go on about how standard list permutations wasn't enough for hyperenvironments, because the elements of the hyperenvironment being environments must also be able to permute within the hyperenvironment, which standard list permutation does not allow, so a deep permutation relation had to be creted to handle this, forshadow the permutation constructors which were required to handled this.  

In \path{Environment/Basic.lean}, the environment is defined as \texttt{Env}, an abbreviation for a standard list of channel-type pairs. By using a list, the formalisation benefits from predictable structural induction, while the unordered and partial properties of the theoretical environment are recovered through explicit equivalence relations and predicates:

\begin{lstlisting}[language=Lean4]
abbrev Elem := (FPName × Types)
abbrev Env := List Elem
\end{lstlisting}

\paragraph{Recovering the PCM Structure}
In linear logic, the environment forms a Partial Commutative Monoid (PCM). Merging two environments ($\Gamma \circ \Delta$) is commutative, associative, and strictly partial—meaning it is only defined if their domains are disjoint. 

Because Lean favors total functions, the merge operation (\texttt{Env.merge}, denoted by the infix operator \texttt{‚}) (``\glq'') is implemented simply as list concatenation (\texttt{++}). Consequently, the raw operation is total and non-commutative. To recover the commutative monoid structure, we instantiate a \texttt{HasPerm} typeclass using Lean's built-in list permutation relation (\texttt{List.Perm}, denoted $\sim$). \path{Environment/Lemmas.lean} proves the necessary structural laws, \texttt{Env.Perm.comm}... Additional interesting lemmas..., establishing that merged environments are equivalent up to permutation. % FIXME: Mention a couple interesting lemmas

To recover the partiality of the PCM, disjointness is enforced via explicit predicates rather than failing the merge operation itself. The function \texttt{Env.names} maps the environment to a \texttt{Finset FPName}, allowing the formalisation to leverage Lean's built-in \texttt{Disjoint} typeclass. A well-formed environment must also be internally linear, meaning no channel is assigned multiple types. This is enforced by the \texttt{Env.Nodup} predicate, which asserts that the list of extracted names contains no duplicates:

\begin{lstlisting}[language=Lean4]
def Env.names (Γ : Env) : Finset FPName := (Γ.map Prod.fst).toFinset
def Env.disjoint (Δ Γ : Env) : Prop := Disjoint Δ.names Γ.names
def Env.Nodup (Γ : Env) : Prop := (Γ.map Prod.fst).Nodup
\end{lstlisting}

The relationship between merging, permutation, and disjointness is heavily exercised in the metatheory. For example, \texttt{Env.Nodup\_merge\_iff} proves that merging two environments preserves linearity if and only if both original environments are linear and strictly disjoint from one another.

\paragraph{Server Usability}
A critical aspect of mechanising the calculus is handling the structural requirements of exponential session types (e.g., servers and clients). In the theoretical presentation of \citep{abs-2106-11818}, the typing rule for server replication requires that all dependencies ($z_1 \dots z_n$) possess exponential types ($\query B_1 \dots \query B_n$). While paper proofs can rely on notation to represent an arbitrary number of such dependencies, this is not directly representable within a strictly typed inductive constructor in Lean.

To formalise this constraint, the mechanisation abstracts the arbitrary list of dependencies into a standard environment ($\Gamma$) and introduces the explicit \texttt{Env.serverUsable} predicate (denoted $?_e\Gamma$). This predicate mandates that every type in the given environment satisfies the \texttt{isServerUsable} condition. This approach provides a mathematically verifiable mechanism to enforce exponential typing conditions over a set.

\paragraph{Lifting Structural Operations}
Finally, because the environment is the outer container for types, all locally nameless operations defined in \cref{subsubsec:stypes} must be lifted. \texttt{Env.lc} asserts that every type in the list is locally closed at a given depth. Similarly, type substitution (\texttt{Env.substTypes}) and De Bruijn shifting (\texttt{Env.shiftTypes}) are implemented by mapping the respective operations across the second element of every pair in the list. As proved in \path{Environment/Lemmas.lean}, these operations perfectly preserve the structural integrity of the environment, including its names, disjointness, and permutations.





\subsubsection{Typing Judgements}\label{subsubsec:judgements}
% The inductive
% How cofinite quantification is encoded in Lean
% Inversion lemmas, linearity, Names property or something
% Show some examples














\subsection{Operational Semantics}
The \texttt{Semantics} namespace covers the operational semantics of \pimll{}. Transition labels are defined in \texttt{Label.lean}. The LTS over typing derivations is defined in \texttt{JudgementStep/}, and the induced LTSs over processes and typing environments are derived in \texttt{ProcStep/} and \texttt{EnvStep/} respectively, realising the erasure homomorphisms described in~\cref{subsec:operational-semantics}.

\subsection{Session Fidelity}
The \texttt{SessionFidelity} namespace establishes the 
results for \pimll{}. It contains the simulation results showing \texttt{EnvStep} and \texttt{ProcStep} can each mimic \texttt{JudgementStep}, subject reduction, which states that if \hl{$\phl{P}$} is well-typed and takes a step to \hl{$\phl{P'}$}, then \hl{$\phl{P'}$} is well-typed under a stepped environment, and typability, which lifts subject reduction to the level of typing derivations, showing that a well-typed process and its typing environment step together coherently.




















\newpage
% FIXME: In chapter 4 when talking about the Basic.lean for types maybe mention why both atom and TVar exist (differentiate between substitution targets (dynamic) and unit types (rigid))
% FIXME: Consider if substitution should be mentioend in section 3, or if it's fine to just mention it in section 4.
% FIXME: Be consistent in regard to calling it a type or a type payload
\begin{center}
\small
\begin{tabularx}{\textwidth}{l X}
    \toprule
    \textbf{ITEMS} & \textbf{TODO} \\
    Writing  & Turn \texttt{Note} paragraphs into \texttt{remark} environments \\
    Writing & Remove highlighting from process names not being part of a process in Background and BindingRepresentation \\
    Writing  & Fix content spread between the Binding Representation and Formalisation sections \\
    Writing  & Hold off on explaining rule side conditions too deeply in background; defer to formalisation chapter \\
    Writing  & In the metatheory/proof strategies section, discuss the structural vs.\ step induction dilemma for erasure \\
    LaTeX    & Fix \cref{prop:name-respect} reference in the section on \texttt{Typing\_f\_eq\_names} \\
    LaTex & Make appendix of the full inductives from Lean \\
    \bottomrule
\end{tabularx}
\end{center}

% \def\bpn{\text{\$N}}
% \def\fpn{\text{\#N}}
% \def\tv{\text{\$T}}